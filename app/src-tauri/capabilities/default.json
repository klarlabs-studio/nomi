{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main Nomi window. Custom commands (#[tauri::command]) registered via invoke_handler don't need capability entries; only plugin commands invoked from JavaScript do.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "shell:allow-spawn",
    "notification:default"
  ]
}
